" 显示行号
set number
set relativenumber

" 搜索时忽略大小写
set ignorecase

" 只要搜索词中包含大写字母，就自动切换回大小写敏感
set smartcase

" --- 基础设置 beg ---
" 将 vim 默认寄存器与系统剪切板共享
set clipboard=unnamed

" --- 基础设置 end ---

" 增量搜索，输入时就开始跳转
set incsearch
