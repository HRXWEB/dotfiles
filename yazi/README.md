# Yazi Config

## Files

- `yazi.toml` — main configuration
- `keymap.toml` — key bindings
- `package.toml` — package manager settings
- `plugins/` — installed plugins

## Sync with stow

```bash
stow --target=$HOME/.config/yazi yazi
```
